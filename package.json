{
  "name": "tilde-path",
  "version": "1.0.0",
  "description": "Convert a path to an absolute tilde path",
  "repository": "shinnn/tilde-path",
  "author": "Shinnosuke Watanabe (https://github.com/shinnn)",
  "scripts": {
    "pretest": "eslint --fix --format=codeframe index.js test.js",
    "test": "node --throw-deprecation --track-heap-objects test.js",
    "coverage": "istanbul cover --print=both test.js"
  },
  "license": "Unlicense",
  "files": [
    "index.js"
  ],
  "keywords": [
    "file",
    "path",
    "resolve",
    "absolute",
    "tilde",
    "tildify",
    "tildeify",
    "convert",
    "shorten",
    "simplify"
  ],
  "dependencies": {
    "tildify": "^1.2.0"
  },
  "devDependencies": {
    "@shinnn/eslint-config-node": "^3.0.0",
    "eslint": "^3.12.2",
    "istanbul": "^0.4.5",
    "tape": "^4.6.3"
  },
  "eslintConfig": {
    "extends": "@shinnn/node"
  }
}
